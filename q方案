# 实施“基于事务日志的原子性回滚与精准清理”方案

针对用户提出的“精准防误删”和“失败回滚”的高级需求，将构建一套基于 `globalState` 的事务日志系统，实现下载任务的强一致性管理。

## 核心机制：事务生命周期 (Transaction Lifecycle)

### 1. 事务记录 (Transaction Record)

在 `globalState` 中维护 `qqq.transactions` 列表。每个事务包含：

* `id`: 唯一标识

* `targetDir`: 下载目录

* `expectedFiles`: 预期下载的文件路径列表（精准匹配）

* `createdAt`: 创建时间戳

### 2. 阶段一：注册 (Register)

* 在 `start()` 确定下载任务后，立即注册事务，记录所有预期生成的文件名（`destPath`）。

* 这确立了“哪些文件属于本次任务”的强绑定关系。

### 3. 阶段二：回滚与清理 (Rollback & Cleanup)

* **场景A：插入失败 (Runtime Failure)**

  * 如果最终 `wsEdit.applyEdit` 失败（如 Tab 关闭且文件被删），立即触发回滚：

  * 根据 `expectedFiles`，删除对应的 `.mp4` 以及其衍生的 `.part`, `.ytdl` 文件。

* **场景B：进程强杀/重启 (Crash Recovery)**

  * 在 `qqq.js` 启动时 (`activate`) 检查残留事务。

  * 对于过期的事务（如 > 10分钟），视为意外中断。

  * 执行延迟回滚：精准删除记录在案的 `expectedFiles` 及其衍生临时文件。

  * **绝不误伤**：只删记录里的文件，目录里其他任何 `.part` 文件均不动。

### 4. 阶段三：提交 (Commit)

* 只有当 `wsEdit.applyEdit` 成功，且光标移动/提示完成后的 `finally` 块中，才从 `globalState` 中移除该事务。

* 这标志着资源的所有权正式移交给用户（文档引用已建立）。

## 代码变更范围

1. **`src/VideoDownloadController.js`**:

   * 新增 `TransactionManager` 类（内部类或独立），封装 `save`, `complete`, `rollback` 逻辑。

   * 改造 `start` 和 `_fastProcess` 流程，在关键节点调用事务方法。

   * 确保 `expectedFiles` 能覆盖 `yt-dlp` 可能生成的临时后缀。

2. **`src/qqq.js`**:

   * 在 `activate` 中调用 `VideoDownloadController.recoverTransactions(context)`。

## 预期行为

1. **强一致性**: 要么“文件在盘上且链接在文档里”，要么“什么都没有”。
2. **零垃圾**: 即使拔掉电源，下次重启 VS Code 时，那次未完成下载留下的 `xxx.mp4.part` 也会被自动清除。
3. **零误伤**: 旁边放着用户自己的 `my_backup.part` 文件，因为不在事务记录里，绝对安全。

用户提出了非常深刻且专业的见解，指出了两个关键点：

误删风险: 不能无脑删 .part 等文件，因为用户可能真的在用这些后缀的文件。必须建立任务与文件的强绑定关系。
原子性回滚: 如果最终“插入暗号”失败（比如文件被删了），应该回滚（删除）所有已下载的落盘文件，保证“要么全成功，要么全不发生”。
这正好与我之前的“原子化粘贴”思路一脉相承，但这次是在持久化层面做原子性保障。

修正后的终极方案：基于“事务日志”的强一致性管理
我们不记录“目录”，而是记录“事务 (Transaction)”。

1. 事务结构 (Transaction Structure)
在 globalState 中维护一个事务列表，每个事务包含：

id: 唯一任务ID
targetDir: 目标目录
startTime: 任务开始时间
tempFiles: 已生成的临时文件列表 (如 xxx.mp4.part, xxx.mp4.ytdl) —— 这是关键，精准定位，绝不误杀。
landedFiles: 已落盘的最终文件列表 (如 xxx.mp4) —— 用于回滚。
status: pending (进行中) | committing (正在插入)
2. 生命周期管理
Step 1: 开启事务 (Begin)

下载开始前，创建事务记录。
globalState.update('qqq.transactions', { ...newTrans })
Step 2: 追踪文件 (Track)

在下载过程中，每当 yt-dlp 或下载器生成一个文件路径（哪怕是临时的），都实时更新到该事务的 tempFiles 列表中。
注：这需要 hook 下载器的 progress 或 file creation 事件，或者简单点，根据 URL 预估文件名并记录前缀。
修正：更简单可靠的方式是，利用 ChildProcessTracker 的文件追踪能力，或者在下载结束后、插入前，扫描目录找出所有相关的“新”文件（通过时间戳+文件名模式）记录下来。
Step 3: 提交阶段 (Commit)

下载成功，准备插入暗号。
将状态更新为 committing。
记录最终生成的 landedFiles。
Step 4: 完成/回滚 (Finish/Rollback)

插入成功: 删除该事务记录。Done.
插入失败 (文件被删/报错): 执行回滚 —— 根据 landedFiles 删除已下载的视频。
进程强杀/重启:
启动时扫描事务列表。
发现 pending 或 committing 的过期事务（比如 > 1小时）。
执行回滚: 根据记录的 tempFiles 和 landedFiles 精准删除文件。
3. 关于“时间关联性” vs “强绑定”
用户提到用“时间关联性”，这在重启后不可靠（文件时间可能变）。 强绑定是最佳方案：我们在下载时就明确知道我们在下载什么文件（文件名通常基于 Title 或 URL hash）。 在 VideoDownloadController 中，我们生成 destPath 时就知道文件名了。我们将这个确切的文件路径记录到事务中。

更佳方式: 在 _createTask 时，生成一个唯一的 taskId。 所有临时文件命名带上这个 ID？不，yt-dlp 不支持乱改名。 但我们可以记录 targetDir + filename_prefix。 或者，最简单有效的方式：记录我们打算下载的文件名。

实施细节修正: 由于 yt-dlp 会自动处理后缀（.mp4 -> .mp4.part），我们记录主文件名即可。清理时，检查 主文件名 + .part / 主文件名 + .ytdl 是否存在且属于该事务时间窗口。

这就满足了用户的要求：

精准清理: 只清理清单里的文件及其衍生临时文件，绝不碰无关的 .part。
完善回滚: 只要插入这一步没过，所有落盘文件全部抹杀。



 你上方一切 ok,  但加入了事物让一切变重了,  故我要你在用户滴全局设置里加一个新滴下拉框二选一选项：“事物包裹级别”  选项一“全包裹” 选项二“半包裹”  默认选项二 . 我记得原来代码里有那样一套逻辑 就是给 包含“混合文字和图片”滴富文本 还有大于80m 滴文件粘贴 等等 给一个进度条让用户能反悔,  那我们半包裹滴意思就是 只包裹 类似 “包含混合文字和图片滴富文本 还有大于80m 滴文件粘贴 等等” 滴耗时任务, 其余一切都不套事物不做保护。  而用户如果选了全包裹 则不区分任何 一切通 通都套上事物包裹 。 最终, 只有套了事物包裹滴 才会给进度条 反悔机会, 其余（如用户就选择半包裹） 不套事物滴一律最快速度响应和处理, 尽量快, 但不给出错回滚也不管失败情形。  可以直接做了 不用计划了
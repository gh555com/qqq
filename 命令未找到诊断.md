# 🔧 命令未找到问题诊断

## ❌ 错误信息
```
command 'qqq.q1' not found
command 'qqq.q2' not found
```

## 🔍 可能的原因

### 1. VSCode 缓存了旧的扩展代码 ⚠️
**最常见原因！**

**解决方法：**
```
在"扩展开发主机"窗口中：
1. 按 Ctrl+Shift+P
2. 输入 "Developer: Reload Window"
3. 按 Enter

或者：
1. 关闭"扩展开发主机"窗口
2. 回到主窗口
3. 重新按 F5 启动调试
```

---

### 2. 模块导出问题 ✅ 已排除

检查结果：
- ✅ src/qqq.js 导出了 `activate` 和 `deactivate`
- ✅ src/q1.js 导出了 `activate`  
- ✅ src/q2.js 导出了 `activate`
- ✅ qqq.js 正确调用了 `q1.activate(context)` 和 `q2.activate(context)`

---

### 3. package.json 配置问题 ✅ 已排除

检查结果：
```json
{
  "main": "./src/qqq.js",  // ✅ 正确
  "activationEvents": [
    "onCommand:qqq.q1",    // ✅ 正确
    "onCommand:qqq.q2"     // ✅ 正确
  ],
  "contributes": {
    "commands": [
      {
        "command": "qqq.q1",   // ✅ 正确
        "title": "q1"
      },
      {
        "command": "qqq.q2",   // ✅ 正确
        "title": "q2"
      }
    ],
    "keybindings": [
      {
        "command": "qqq.q1",  // ✅ 正确
        "key": "ctrl+v"
      },
      {
        "command": "qqq.q2",  // ✅ 正确
        "key": "f2"
      }
    ]
  }
}
```

---

### 4. 命令注册代码问题 ✅ 已排除

**src/q1.js:**
```javascript
function activate(context) {
	context.subscriptions.push(
		vscode.commands.registerCommand("qqq.q1", executeClipboardCommand),  // ✅
		vscode.commands.registerCommand("qqq.toggleBlockMode", toggleBlockMode),
		vscode.commands.registerCommand("qqq.openFile", openFileCommand),
		// ...
	);
}
```

**src/q2.js:**
```javascript
function activate(context) {
	context.subscriptions.push(
		vscode.commands.registerCommand("qqq.q2", showSaveAsDialog),          // ✅
		vscode.commands.registerCommand("qqq.saveAsDialog", showSaveAsDialog)
	);
}
```

---

## 🚀 立即解决步骤

### 方法 1：重新加载窗口（推荐）

1. 确保"扩展开发主机"窗口是活动窗口
2. 按 `Ctrl+Shift+P`
3. 输入 `reload`
4. 选择 **"Developer: Reload Window"**
5. 等待窗口重新加载
6. 尝试按 `Ctrl+V` 或 `F2`

---

### 方法 2：重启调试

1. 关闭"扩展开发主机"窗口
2. 在主窗口（项目文件夹）中
3. 按 `F5` 重新启动调试
4. 等待新的"扩展开发主机"窗口打开
5. 尝试按 `Ctrl+V` 或 `F2`

---

### 方法 3：查看调试日志

现在 qqq.js 已添加调试日志，查看输出：

1. 在主窗口按 `Ctrl+Shift+Y` 打开调试控制台
2. 查看是否有以下输出：
   ```
   qqq 扩展激活中...
   q1 module: { activate: [Function] }
   q2 module: { activate: [Function] }
   q1.activate 执行完成
   q2.activate 执行完成
   所有命令注册完成
   ```

3. 如果看到错误信息，请截图发送

---

### 方法 4：检查扩展是否激活

1. 在"扩展开发主机"窗口按 `Ctrl+Shift+P`
2. 输入 `qqq`
3. 应该看到：
   - ✅ q1
   - ✅ q2
   - ✅ qqq: 新建文件...
   - ✅ 切换块模式

如果看不到这些命令，说明扩展没有激活。

---

## 🧪 测试命令是否注册成功

### 测试 qqq.q1
```
1. 打开任意文本文件
2. 复制一张图片到剪贴板
3. 按 Ctrl+V
4. 应该粘贴图片路径并显示装饰
```

### 测试 qqq.q2
```
1. 在任意编辑器窗口
2. 按 F2
3. 应该打开"新建文件"对话框
```

---

## 📝 如果问题仍然存在

请提供以下信息：

1. **调试控制台输出**
   - 按 Ctrl+Shift+Y
   - 截图或复制所有输出

2. **扩展主机的开发者工具**
   - 在"扩展开发主机"窗口
   - Help > Toggle Developer Tools
   - 查看 Console 标签
   - 截图错误信息

3. **命令面板列表**
   - 按 Ctrl+Shift+P
   - 输入 `qqq`
   - 截图显示的命令列表

---

## ✅ 预期的正常状态

重新加载后，应该看到：

```javascript
// 调试控制台输出
qqq 扩展激活中...
q1 module: { activate: [Function: activate] }
q2 module: { activate: [Function: activate] }
q1.activate 执行完成
q2.activate 执行完成
所有命令注册完成
```

然后：
- ✅ Ctrl+V 可以粘贴图片
- ✅ F2 可以打开新建文件对话框
- ✅ 命令面板能找到 qqq 相关命令

---

**诊断时间**：2025-10-15  
**建议操作**：先尝试"重新加载窗口"（方法1）

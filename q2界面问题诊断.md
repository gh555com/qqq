# q2 界面问题诊断

## 用户反馈

> ok 命令能打开了  q1 功能正常了  但 q2 差太多   你再读读文档和我们滴源码需求  你现在连界面图标，那些都在改 啥叫一模一样？

## 关键问题

用户强调："**一模一样**" 的核心含义：
- 界面图标必须与原版 extension.js 完全一致
- 布局、样式、交互必须完全一致
- 所有视觉元素不能有任何修改

## 原版 extension.js 的 q2 界面元素清单

### 1. 图标 (CRITICAL)
从 extension.js 第 1087, 1110, 1133 行可以看到：
- **文件夹图标**: `📁` (emoji)
- **文件图标**: `📄` (emoji)
- **不是SVG，不是图片，就是emoji字符！**

### 2. 侧边栏（左侧导航）
应该包含：
- 标题："文件导航"
- **盘符按钮列表** (C:\, D:\, E:\, etc.)
- 分割线
- **历史回收站** (conditionally shown)

### 3. 最近保存目录区域
- 每一项前面有 **删除按钮 (×)**
- 点击可以导航到该目录

### 4. 文件列表项结构
每个文件/文件夹项应该包含：
```html
<div class="file-item">
    <div class="file-select-area">     <!-- 点击选中区 -->
        <div class="sz-area"></div>     <!-- 文件大小区 -->
        <span class="file-icon">📁</span> <!-- emoji图标 -->
    </div>
    <div class="folder-name-area">      <!-- 文件夹名，点击导航 -->
        <span class="file-name">xxx</span>
    </div>
</div>
```

### 5. 底部按钮
- **新建文件按钮**: SVG 图标（磁盘保存图标）
- **新建文件夹按钮**: SVG 图标（文件夹图标）
- 长驻按钮

## 当前 q2.html 模板的占位符

应该有以下占位符：
- `{{SIDEBAR_WIDTH}}` - 侧边栏宽度
- `{{LINE_SPACING}}` - 行间距
- `{{DRIVES}}` - 驱动器列表HTML（改成 {{DRIVES_HTML}} 更清晰）
- `{{RECYCLE_BIN}}` - 回收站HTML（改成 {{RECYCLE_BIN_HTML}} 更清晰）
- `{{RECENT_DIRS}}` - 最近目录HTML（改成 {{RECENT_DIRS_HTML}} 更清晰）
- `{{CURRENT_PATH}}` - 当前路径
- `{{CURRENT_PATH_JS}}` - JavaScript中使用的转义路径
- `{{PIN_CLASS}}` - pin按钮class
- `{{PIN_CHECKBOX}}` - pin复选框内容
- `{{SIZE_MODE}}` - 当前size mode
- `{{SIZE_MODE_NONE}}`, `{{SIZE_MODE_M}}`, `{{SIZE_MODE_K}}`, `{{SIZE_MODE_B}}` - size mode选中class

## 检查清单

- [ ] q2.html 模板占位符是否正确（不应该有空格或换行）
- [ ] q2.js 的 getWebviewContent 是否正确替换了所有占位符
- [ ] 图标是否使用emoji `📁` 和 `📄`
- [ ] 侧边栏是否正确生成驱动器按钮
- [ ] 最近目录是否有删除按钮
- [ ] 底部SVG按钮是否正确

## 立即验证步骤

1. 检查 q2.html 模板的占位符格式
2. 检查 q2.js 的 getWebviewContent 函数替换逻辑
3. 验证生成的 HTML 是否与原版完全一致

---

**下一步**: 立即修复 q2.html 和 q2.js，确保 100% 照抄原版 extension.js

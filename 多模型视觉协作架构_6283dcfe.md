# 多模型视觉协作架构

## 核心设计思路

不改变 DeepSeek 作为"大脑"的地位，而是给它配一个"视觉前处理器"。
图片先过视觉模型（通义千问 VL-Plus），产出文本描述，再喂给 DeepSeek 做推理。

这个模式天然适配我们的怒气值架构——Planner 决定任务复杂度，视觉处理作为 Gatherer 阶段的一个工具。

## 架构图

```
用户消息（含图片）
       |
       v
[客户端 agent.js]
  检测到图片附件 → 调用 analyze_image 工具
       |
       v
[gaea 服务端] POST /api/v3/ai/vision
  路由到视觉模型（通义千问 Qwen-VL-Plus）
  返回: 图片文本描述 + OCR 文本 + 结构化信息
       |
       v
[agent.js] 将视觉结果注入 messages 上下文
       |
       v
[gaea 服务端] POST /api/v3/ai/chat
  DeepSeek 看到文本描述，做推理/回答
```

## 为什么这样设计（而不是换一个多模态模型）

1. DeepSeek V4 代码能力顶级，不能因为图片就换掉主脑
2. 通义 VL-Plus 成本极低（1.5元/百万tokens），专门做"眼睛"性价比最高
3. 未来主力模型可能换（GPT-5.5, Claude 4, etc.），但"眼睛"模块独立，随时可换
4. 符合怒气值架构的"分工"哲学：便宜的跑腿，贵的做决策

---

## Task 1: 服务端新增 /api/v3/ai/vision 端点

文件: `E:\s\wol\py\gaea\internal\http\handlers_ai_vision.go`（新建）

功能:
- 接收 base64 图片 + 可选 prompt
- 调用通义千问 Qwen-VL-Plus API（gaea 已有阿里云账号打通）
- 返回 JSON: `{ "description": "...", "ocr_text": "...", "objects": [...] }`

请求体:
```json
{
  "image": "<base64>",
  "prompt": "描述这张图片的内容",  // 可选，默认通用描述
  "detail": "high" | "low"          // high=详细分析, low=快速OCR
}
```

响应:
```json
{
  "description": "图片中是一个VS Code编辑器窗口，左侧显示文件列表...",
  "ocr_text": "Toggle AI Vision\nSelect a folder...",
  "tokens_used": 1200
}
```

## Task 2: 客户端新增 analyze_image 工具

文件: `ai/src/tools.js`

新增工具定义:
```javascript
{
    type: 'function',
    function: {
        name: 'analyze_image',
        description: 'Analyze an image using vision AI. Returns text description and OCR.',
        parameters: {
            type: 'object',
            properties: {
                path: { type: 'string', description: 'Absolute path to the image file' },
                prompt: { type: 'string', description: 'What to look for in the image' }
            },
            required: ['path']
        }
    }
}
```

执行逻辑:
- 读取图片文件 → base64
- POST 到 gaea /api/v3/ai/vision
- 返回文本描述给 DeepSeek

## Task 3: agent.js Planner 感知图片

修改 Planner prompt，加入图片感知:
- 如果用户消息中提及图片/截图/文件名.png 等，Planner 应在 info_needed 中加入 `analyze_image` 工具调用
- 这样 Gatherer 阶段会自动调用视觉 API

## Task 4: 多模型路由抽象层（长期）

文件: `gaea/internal/http/model_router.go`（新建）

设计一个通用模型路由器:
```go
type ModelRequest struct {
    Task     string // "chat", "vision", "embed", "tts"
    Provider string // "deepseek", "qwen", "openai" (auto if empty)
    Input    any
}
```

路由规则:
| 任务类型 | 默认提供商 | 备选 |
|---------|-----------|------|
| chat (代码/推理) | DeepSeek V4 | - |
| vision (图片理解) | 通义千问 VL-Plus | DeepSeek 识图模式(当开放API时) |
| embed (向量化) | 通义千问 text-embedding | OpenAI ada |
| tts (语音) | 保留 | - |

这样未来换模型只改路由表，客户端零修改。

## Task 5: 费用模型扩展

vision 调用的费用:
- 通义 VL-Plus: 键入 1.5 元/百万tokens, 产出 4.5 元/百万tokens
- 一张 720P 图片约 1000 tokens = 约 0.0015 元成本
- 乘以 2x 倍率 = 用户消耗约 0.003 ge（可忽略不计）
- 建议: 图片识别直接免费，不单独计费（算入整体对话消耗）

## Task 6: 更新 system prompt 和交接文档

- system prompt CAPABILITIES 里把"不能识别图片"改为"可以分析图片"
- 交接文档加入多模型协作章节

---

## 优先级排序

1. Task 1 (服务端 vision 端点) — 最核心，解锁能力
2. Task 2 (客户端 analyze_image 工具) — 对接
3. Task 3 (Planner 图片感知) — 自动化
4. Task 5 (费用模型) — 简单
5. Task 6 (文档) — 收尾
6. Task 4 (通用路由器) — 长期重构，不急

## 需要确认

- gaea 服务端通义千问的 API Key 已经配置好了吗？环境变量名是什么？
- 图片传送方式：用户在聊天窗口上传图片（需要前端改 UI），还是只支持通过文件路径分析已有图片？

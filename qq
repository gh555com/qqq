
关键时间线：
- `18:18:09.408` — 启动 Python bridge
- `18:18:27.271` — **Python Bridge 启动失败：Broker connect failed after spawn+retries**（仅18秒后就放弃了）

但你说过了5-10分钟 Python 才起来。这说明第一次启动失败后，有某个**重试/恢复机制**在后面又把它拉起来了。

让我看看 `startDaemons` 和 broker 相关的逻辑：
现在看看 `startDaemons` 的逻辑，这是日志中调用 Python bridge 启动的地方：
现在看看 BrokerBridge 的 `start` 方法和 spawn 逻辑：
Now let me look at how Python eventually starts (the hot-start after download callback):
Now I have the full picture. Let me look at the timing in the log more carefully and check the `dow.js` initialization flow:
日志里显示 `[startDaemons] 已有 bridge 活动中 (py=true, rust=null, shell=null)` — 说明在 startDaemons 被调用时，`pythonBusy=true`（pythonBridge 已经在 starting 中了）。

让我确认 `initPythonBrokerBridge` 的调用时机：
Now let me check the `tryOneByOne` log message and see what waited for engines:
现在看日志时间线完整逻辑：

1. `18:18:03.653` — PythonCheck L1 **完美命中**（python.exe 存在且之前通过检查）
2. `18:18:09.408` — 启动 Python bridge（`_doStart` 开始）
3. `18:18:27.271` — **Python Bridge 启动失败**：Broker connect failed after spawn+retries

关键问题在于：虽然 L1 检查 python.exe 存在（28MB文件夹正常），但 `_spawnBrokerThrottled` 里的 `_checkPythonPerfect()` 可能因为**依赖不完整**（`deps_missing`）或其他原因返回 `ok: false`，导致**没有真正 spawn Python 进程**。

spawn 没发生 → 30次连接重试全失败 → "Broker connect failed after spawn+retries"。

然后5-10分钟后，是 `dow.js` 的 `onPythonReady` 回调触发了 `pythonBridge.start()` 的 **hot-start**（line 664-679）。

让我确认 dow.js 预热的时机：
现在看完整时间线，让我看 dow.js 里 `ensurePythonReady` 怎么触发的以及 `onPythonReady` 的回调：
Now I have the complete picture. Let me also check when `initPythonBrokerBridge` triggers `ensurePythonReady`:

看日志时间线：
- `18:18:03.653` — L1 完美命中 python.exe（`init()` 里的 `initPythonBrokerBridge` 内的 `ensurePythonReady` 检查通过）
- `18:18:09.395` — startDaemons 开始（6秒延迟后）
- `18:18:09.408` — 启动 Python bridge → `_doStart()` → `_tryConnectOnce()` 失败（没有现存 Broker）→ `_spawnBrokerThrottled()`
- `18:18:14.862` — dow.js 预热（w10，即10秒延迟的 setTimeout 触发）
- `18:18:27.271` — Python Bridge 启动失败

关键问题：L1 在 `18:18:03` 检查通过了（`checkL1Perfect` 返回 `perfect=true`），但 18s 后还是失败了。

这意味着 spawn **确实发生了**（因为 `_checkPythonPerfect` 如果 `_downloadedPythonPath` 已设置会直接返回 ok，而且 L1 已经 perfect）。所以 Python 进程确实被启动了，但 **Broker 启动太慢**，30 次重试都没连上。

重试总耗时计算：`CONNECT_RETRY_DELAY_BASE_MS + i * 30`，i 从 0 到 29：
- 第一次：121ms，第二次：151ms，...第30次：121 + 29*30 = 991ms
- 总时长 = Σ(121 + i*30) = 30*121 + 30*29/2*30 = 3630 + 13050 = **约 16.7 秒**

从 spawn 到放弃约 16.7 秒 + spawn 前随机 0-2s + spawn 本身。所以从 `18:18:09` 到 `18:18:27` 约 18 秒吻合。

**根本原因**：Python Broker 进程启动太慢（首次启动需要 import 大量模块），在16.7秒的重试窗口内没来得及 listen endpoint。之后过了几分钟，是**心跳重连**或 `onPythonReady` 回调才把它连上。

让我确认心跳是否有重连逻辑：
但心跳只有在连接成功后才启动（`_startHeartbeat` 在 `_doStart` 成功才调用）。由于第一次 `_doStart` 就失败了，心跳从未启动。

那后来是谁把它拉起来的？回到 line 664-679：`onPythonReady` 回调。但 L1 在 `18:18:03` 就已 perfect 了，意味着 `ensurePythonReady` 直接返回 pythonPath（line 2814-2821），**不会触发 `onPythonReady` 回调**（那个回调只在 `fromScratch` 即新安装时触发 line 2883）。

那到底是什么让 Python 最终起来的？让我看看还有没有其他重试路径：
所以 `pythonBridge.start()` 只被调用两次：
1. `startDaemons` → `ensureStarted(pythonBridge)` — 失败了
2. `onPythonReady` 回调 — 但**前提是 `!pythonBridge.isAvailable()`**

问题：`onPythonReady` 什么时候触发？看 line 636：`downloader.python.onPythonReady(async (pythonPath, context) => {...})`

而 `ensurePythonReady` 在 line 683 被调用：如果 L1 已 perfect，它直接返回 pythonPath 但**不触发 onPythonReady 回调**。

那 `_scheduleInstallCheck` 呢？看看它是否有重连逻辑：
好的，但这个 `_scheduleInstallCheck` 只在 `installInProgress` 或 `install skipped` 时才启动，我们这里 L1 已经 perfect 了，不会走这条路。

那就剩一个问题：**Python 最终是怎么连上的？**

答案应该在 `brokerBridge.js` 的 `call()` 方法 line 172-178：
```js
if (!this.isAvailable()) {
    const ok = await this.start();
    if (!ok) throw...
}
```

任何后续对 `pythonBridge.call()` 的调用都会触发 `start()` 重连。而5-10分钟后，有某个代码（比如剪切板监控、音频等）尝试调用 `pythonBridge.call()`，此时 Python Broker 进程已经启动并 listen，所以 `start()` → `_doStart()` → `_tryConnectOnce()` 成功了。

但问题是：`available = false` 时 `call()` 还会 start 吗？看 line 422-425：

```js
if (this.available === false) {
    return { error: `${this.name}_not_available` };
}
```

这是 DaemonBridge 的逻辑。BrokerBridge 继承的是 `call()` 方法 line 171，它不检查 `available`，直接用 `isAvailable()`（检查 socket）。所以**确实会尝试 reconnect**。

等等，看 line 171-178 更仔细：`this.isAvailable()` 返回 false → 调 `this.start()` → 进入 `_doStart()` → 又来一轮 spawn + 30 次重试。但此时 Python Broker 已经在跑了（第一次 spawn 的那个进程），所以 `_tryConnectOnce` 会成功。

**根因确认**：Python Broker 进程首次冷启动慢（需要 import 大量模块如 numpy 等），在约 17 秒的重试窗口内没来得及 ready。之后某次 `call()` 或其他触发点再次尝试连接时才成功。

**但为什么要等 5-10 分钟？** 因为：
1. 第一次 `_doStart` 失败后 `available = false`
2. 没有心跳（心跳没启动）
3. 没有自动重连定时器
4. 唯一的重连机会是有代码调用 `pythonBridge.call()`
5. `call()` 里确实有 `start()` 重连逻辑
6. 那是什么在调 `call()`？

看 wq reporter（line 11: `[wq] Reporter started, initial ping in 69s`）或剪切板监控：
这里有 `if (pythonBridge.isAvailable())` 保护，所以不会在不可用时调。

那 `tryOneByOne` / `tryEngineCall` 呢？当用户做 IO 操作时可能会触发 Python：
`tryOneByOne` 使用 `getEffectiveEngineOrder()` 来确定可用引擎。由于 `available = false`，Python 不在列表里。所以 IO 操作也不会触发 Python reconnect。

那到底是什么？让我看看 `qqq.js` 里的音频播放逻辑，用户操作时可能直接调了 `pythonBridge.call()`：
Line 1852-1854！这里有：
这个只在用户切换引擎时触发。不是自动的。

那真正的答案是：**`brokerBridge.js` 的 `call()` 方法**。line 172-178：
```js
if (!this.isAvailable()) {
    const ok = await this.start();
    ...
}
```

但谁在不可用时还调 `call()`？ 看看 `qqq.js` line 1063 附近：
这里有 `bridge.available !== true` 的保护。所以正常路径都不会在不可用时调用。

那最终是什么让它在5-10分钟后连上的？我怀疑是用户在操作时触发了某个功能（如播放音频），通过 `call()` 的 reconnect 逻辑或直接通过 `start()` 再次连上。

但等等，让我重新看一下 `initPythonBrokerBridge` 里的 flow：

```
line 683: const pythonPath = await downloader.ensurePythonReady(extensionContext);
```

这个 `ensurePythonReady` 是在 `initPythonBrokerBridge` 里被异步调用的，但它在 `startDaemons` 之前（init → initPythonBrokerBridge 在 activate 时，而 startDaemons 是 6 秒后）。

由于 L1 已 perfect，`ensurePythonReady` 直接返回 pythonPath，然后 line 684-685 设置 `pythonBridge._downloadedPythonPath = pythonPath`。这发生在 startDaemons 之前，所以 `_doStart` 里的 `_checkPythonPerfect` 能通过，spawn 能执行。

**关键发现**：Python Broker 确实被 spawn 了（因为检查通过），但进程冷启动耗时超过 17 秒（30 次重试的总窗口），所以第一次连接全部失败。

而之后 5-10 分钟才连上的原因，我需要看看是否有隐藏的恢复路径。让我看看 `_onDisconnected`：
OK，没有自动重连的定时器。那就说明 Python 最终是通过某个用户操作或功能触发 `call()` → `start()` 再次成功连接的。

但用户说"大概等到10分钟"他只是在等，没做什么操作。那么…

让我重新审视一下 `_spawnBrokerThrottled` 中的 stderr 处理，看看 spawn 的进程有没有报错：
好了，我现在完全理解了。总结：

---

**根因定位**：

1. **Python Broker 进程冷启动太慢** — 首次 spawn 后，Python 需要 import 大量模块（miniaudio、各种依赖），实际启动到 listen 可能需要 20-30+ 秒
2. **重试窗口太短** — `_doStart` 只有约 17 秒的重试窗口（30 次 × 平均 560ms），在 Python 还没 ready 时就放弃了
3. **失败后没有自动恢复机制** — `available = false` 后，没有定时重连（心跳只有连接成功才启动），也没有后台重试
4. **5-10 分钟后恢复** — 可能是某个功能通过 `call()` 方法的 reconnect 逻辑触发了 `start()` → 此时 Python 早已 ready → 一次 `_tryConnectOnce` 就连上了

**修复方案**：在 `_doStart` 失败后，启动一个**延迟重连定时器**（比如每 30 秒尝试一次，最多尝试 10 次 = 5 分钟内覆盖），而不是彻底放弃。
Also need to clean up the timer on `stop()`/`dispose()`:
**原因定位 + 修复完毕。**

---

**总结：**

**根因**：Python Broker 进程首次冷启动（import miniaudio + 依赖）需要 20-60 秒，但 `_doStart()` 的重试窗口只有约 17 秒（30 次 × 递增延迟）。超时后设置 `available=false`，此后**没有任何自动重连机制**。5-10 分钟后恢复是因为某个后续操作偶然触发了 `call()` → 内部 `start()` 重连。

**修复**：在首次连接失败后，启动一个**延迟重连定时器**（每 15 秒尝试连接一次，最多 20 次 = 5 分钟覆盖）。由于 Python 进程已经在后台跑着了（spawn 成功），通常在第 1-3 次重试（15-45 秒内）就能连上，不用再等几分钟靠运气。
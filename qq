我突然想到个问题， 就是我们返回滴在线人数应该  逻辑上应该更新了，  我们之前是定死滴逻辑 就是返回 只返回最近12小时在线滴， 你觉得我们现在是不是可以改到 去适配 就是刚才在 ​ruler.pyw​ 里面加滴那个唯一真理值， 比如现在是15分钟， 我们前端 和聚合程序 都会自动适配到 聚合和展示 最近15分钟在线滴， 前端滴更新时间还是可以保持5分钟不变， 而客户端那边滴 显示也更新到 最近15分钟在线滴， 你觉得那样做是更好还是更坏？ 难度大吗？ 最重要滴是， 我想对于 偿还给自己 设计一套完全不同滴逻辑，  偿还给自己 无论用户点播放按钮 还是循环播放按钮 都会立即 ping 上报一次， 但是客户端做了一个防抖， 就是5分钟最多 ping 上报一次， 服务器也是一样 5分钟最多 记录一次， 再有更多直接丢掉， 反正就是防止攻击者打服务器打数据库， 这一块安全你要做到极致， 那接下来 我们返回给客户端滴 正在偿还给自己滴人， 你觉得应应该怎样设计 统计滴时间窗口 ，该窗口又像之前那样是一个定死滴固定值， 还是一样设计为灵活滴可控值？ 你觉得终极滴架构应该是怎样？







好问题，让我直接给你架构分析：

## 在线人数窗口

**现状**：12h 硬编码（当初 ping 间隔是 12h，所以 12h 窗口刚好 = 错过一次就掉线）

**改法**：窗口 = `2 × PING_INTERVAL_MINUTES`。ping 间隔 15min → 窗口 30min。改到 1h → 窗口 2h。逻辑自洽：**容忍错过一次 ping，第二次还没来就算离线**。

难度：很低。PG 函数里的 `interval '12 hours'` 改成动态参数就行。

## 正在偿还（active_playing）

这是完全不同的信号通道：

| 维度 | 普通 ping | 偿还 ping |
|------|-----------|-----------|
| 触发 | 定时器（15min） | 用户动作（点播放/循环） |
| 客户端防抖 | 15min | **5min** |
| 服务端限速 | 1min/device | **5min/device** |
| 统计窗口 | 2×15min=30min | **2×5min=10min** |

**窗口应该是固定值（10 分钟）**，不需要跟 `PING_INTERVAL_MINUTES` 联动。原因：
- 5min 防抖是客户端硬编码的行为特征，不会像 ping 间隔那样频繁调整
- 如果也做成可配，两个旋钮互相耦合反而容易出错
- 10min 已经足够紧——停止偿还 10 分钟后就从"正在偿还"消失

## 终极架构建议

```
客户端:
  定时 ping (每 PING_INTERVAL_MINUTES 分钟)
    → POST /api/goods/:slg/wq/ping  { playing: false }
    → 服务端限速: 1min/device

  偿还动作 ping (点播放/循环，5min防抖)
    → POST /api/goods/:slg/wq/ping  { playing: true }
    → 服务端限速: 5min/device（独立限速桶）

服务端:
  device_state.last_seen_at      → 每次 ping 都更新（在线窗口用）
  device_state.last_playing_at   → 仅 playing=true 时更新（偿还窗口用）

  在线人数:  WHERE last_seen_at    > now() - 2×PING_INTERVAL_MINUTES
  偿还人数:  WHERE last_playing_at > now() - interval '10 minutes'
```

**关键安全点**：
- 两个独立的 `sync.Map` 限速桶（普通 ping 和偿还 ping）
- 偿还 ping 的限速桶 key = `play:{good_int}:{device_id}`，5min TTL
- 被限速的请求返回 200 OK 但不写 DB（跟现有普通 ping 一致）
- `playing` 字段是 bool，攻击者伪造也只是多写一个时间戳，无大害

要我开始实现吗？改动量不大：
1. `device_state` 加 `last_playing_at` 列
2. ping handler 读 `playing` 字段 + 独立限速桶 + 条件更新
3. PG 函数的 `12 hours` → 动态参数
4. stats 响应的 `active_playing` 查询改用 `last_playing_at`



先搜集需要改的代码段：
Now I also need to see the PG functions for version/IDE/publisher stats (they also hardcode 12 hours) and the city online stats:
好，信息充分了。开始实施。

**Step 1: Migration** — `device_state` 加 `last_playing_at` + 重写所有 PG 函数接受动态窗口参数
**Step 2: ping handler** — 加 `Playing` 字段、独立限速桶、条件更新 `last_playing_at`
加独立偿还限速桶：
Now I need to add `last_playing_at` update logic in the ping handler's INSERT and UPDATE SQL. Let me read the exact SQL context:
I need to add the playing rate limit logic and modify the SQL. Let me add the playing check after the transaction starts but before the SQL:
**Step 3: RefreshCountryOnlineStats** — 改 PG 函数调用传入动态窗口参数
Now update the city, version, IDE, publisher function calls to also pass the window:
Now update all 4 remaining PG function calls to pass `onlineWindowMinutes`:
Now handle `RefreshCityOnlineStats`:
**Step 4: stats 响应** — `active_playing` 改用 `last_playing_at` + 10min 窗口
编译通过。
编译通过，全部改动总结：

---

**Migration** — [0022_playing_dynamic_window.up.sql](file:///e:/s/wol/py/gaea/migrations_analytics/0022_playing_dynamic_window.up.sql)
- `device_state` 加 `last_playing_at TIMESTAMPTZ`（NULL=从未偿还）
- 索引 `idx_device_state_last_playing` 加速 10min 窗口查询
- **5 个 PG 函数全部改为接受 `p_window_minutes` 参数**（默认 30），不再硬编码 12h/13h

**handlers_wq.go** — [handlers_wq.go](file:///e:/s/wol/py/gaea/internal/http/handlers_wq.go)
- `wqPingRequest` 加 `Playing bool` 字段
- 新增 `wqPlayingRateLimiter`（独立 `sync.Map`，5min/device 防抖）
- ping 写入时：`playing=true` 且未被限速 → 更新 `last_playing_at`；否则保留旧值（`COALESCE($34, last_playing_at)`）
- `RefreshCountryOnlineStats` + City/Version/IDE/Publisher 全部传入 `onlineWindowMinutes = PingIntervalMinutes × 2`
- stats 响应 `active_playing` 改用 `last_playing_at > now() - 10min`

**安全设计**：
| 层 | 防御 |
|---|---|
| 客户端 | 5min 防抖（播放/循环触发后，5min 内不重复 ping） |
| 服务端内存 | `wqPlayingRateLimiter` sync.Map，5min TTL/device |
| 服务端 DB | 被限速时返回 200 但不写 DB，零 DB 压力 |
| 普通 ping | 原有 `wqDeviceRateLimiter` 1min 限速不变 |

**两个信号通道完全解耦**：
- **在线人数**：`last_seen_at` + 动态窗口 `2 × PING_INTERVAL_MINUTES`（ruler.pyw 改间隔自动适配）
- **偿还人数**：`last_playing_at` + 固定 10min 窗口（跟随客户端 5min 防抖，不受 ping 间隔影响）


# 🚀 快速启动指南

## 项目已就绪！

✅ 所有依赖包已安装（133个）
✅ 配置分隔符问题已修复
✅ 代码无语法错误
✅ 项目结构完整

---

## 立即测试

### 方法 1：在 VSCode 中调试（推荐）

1. **打开项目**
   ```
   在 VSCode 中打开文件夹：E:\s\wol\py\q3
   ```

2. **启动调试**
   ```
   按 F5 键
   或
   运行 > 启动调试
   ```

3. **测试功能**

   在新打开的"扩展开发主机"窗口中：

   ```
   按 F2              → 打开"新建文件"对话框
   Ctrl+V             → 粘贴图片（需先复制图片）
   Ctrl+Shift+P       → 输入 "qqq" 查看所有命令
   ```

### 方法 2：打包安装

1. **生成 VSIX 文件**
   ```bash
   cd E:\s\wol\py\q3
   npm run package
   ```

2. **安装扩展**
   ```bash
   code --install-extension qqq-1.0.0.vsix
   ```

---

## 核心功能快捷键

| 快捷键 | 功能 | 说明 |
|--------|------|------|
| `F2` | 新建文件对话框 | 主要功能，本次修复重点 |
| `Ctrl+V` | 粘贴图片 | 自动调整为 512x288 |
| `Q` | 重命名（对话框内） | 选中文件后按 Q |
| `W` | 打开文件（对话框内） | 用默认程序打开 |
| `S` | 删除（对话框内） | 移到回收站 |
| `E` | 编辑（对话框内） | 在 VSCode 中打开 |
| `Backspace` | 返回上级（对话框内） | 导航到父目录 |

---

## 验证修复是否成功

### ✅ 检查点 1：历史区显示
```
打开 F2 对话框
→ 右上角应该显示"历史"区域
→ 如果之前有使用记录，会显示最近的目录
```

### ✅ 检查点 2：文件列表显示
```
打开 F2 对话框
→ 中间区域应该显示当前目录的文件和文件夹
→ 文件夹显示 📁 图标
→ 文件显示 📄 图标
```

### ✅ 检查点 3：侧边栏拖动
```
打开 F2 对话框
→ 左侧有驱动器列表（C:, D: 等）
→ 鼠标悬停在分割线上会变成双向箭头
→ 可以拖动调整宽度
```

---

## 如果界面仍然空白

### 步骤 1：检查配置文件
```powershell
Get-Content E:\r\pz.ini
```

应该看到：
```ini
[qqq]
recent_dirs=路径1,路径2
line_spacing=-2
sidebar_width=100
```

**注意**：路径之间用 **逗号** 分隔，不是分号！

### 步骤 2：查看控制台错误
```
在扩展开发主机窗口中：
Help > Toggle Developer Tools
→ 查看 Console 标签页
→ 截图发送给我
```

### 步骤 3：重新加载
```
Ctrl+Shift+P
→ 输入 "reload"
→ 选择 "Developer: Reload Window"
```

---

## 配置文件位置

**全局配置**：`E:\r\pz.ini`
- 多个程序共享
- [qqq] 区域是本扩展的配置
- 使用逗号分隔数组值

**日志文件**：`E:\r\qqq.log`
- 记录扩展运行日志
- 可用于调试

---

## 常用命令

### 开发模式
```bash
# 安装依赖
npm install

# 打包扩展
npm run package

# 发布扩展（需要 token）
npm run publish

# 运行测试
npm test
```

### 故障排查
```bash
# 重装依赖
Remove-Item node_modules -Recurse -Force
Remove-Item package-lock.json
npm install

# 查看 sharp 状态
npm list sharp

# 清理缓存
npm cache clean --force
```

---

## 项目结构速览

```
E:\s\wol\py\q3\
├── src/                    # 源代码
│   ├── qqq.js             # 主入口 ⭐
│   ├── q1.js              # 粘贴图片功能
│   ├── q2.js              # 新建文件对话框 ⭐
│   ├── q2.html            # 对话框 UI 模板
│   ├── ConfigManager.js   # 配置管理
│   └── kp.py              # 图片处理脚本
│
├── assets/                 # 资源文件
│   └── icon.png           # 扩展图标
│
├── node_modules/           # 依赖包（133个）
│
├── package.json            # 项目配置 ⭐
├── package-lock.json       # 依赖锁定
├── .vscodeignore          # 打包忽略规则
│
└── 文档/
    ├── 项目初始化报告.md
    ├── 问题修复报告.md
    ├── ConfigManager使用指南.md
    └── 初始化检查清单.md
```

---

## 需要帮助？

如果遇到问题，请提供：

1. **截图**：界面显示情况
2. **配置**：`E:\r\pz.ini` 的内容
3. **控制台**：Developer Tools 的 Console 输出
4. **操作**：详细描述做了什么

---

**准备时间**：2025-10-15
**状态**：✅ 就绪
**下一步**：按 F5 启动调试！

# q2 文件列表未显示问题调试

## 问题

用户反馈："文件列表还是没出来，功能差太远"

## 已添加的调试日志

### Node.js 端（q2.js updateResourceExplorer 函数）

```javascript
console.log('updateResourceExplorer: 开始更新，currentPath =', currentPath);
console.log('directoryContents:', {
    dirs: directoryContents.dirs.length,
    files: directoryContents.files.length
});
console.log('updateResourceExplorer: 发送 update 消息，items =', items.length);
console.log('fileListHtml 预览:', fileListHtml.substring(0, 200));
console.log('updateResourceExplorer: update 消息已发送');
```

### Webview 端（generateWebviewScript）

```javascript
console.log('webview 收到消息:', message.command);
console.log('webview: 处理 update 消息');
console.log('  currentPath:', message.currentPath);
console.log('  fileListHtml 长度:', message.fileListHtml.length);
console.log('  items 数量:', message.items.length);
console.log('webview: fileList innerHTML 已设置');
console.log('webview: update 处理完成');
```

## 测试步骤

1. **保存所有文件** (Ctrl+K S)

2. **重新加载窗口**
   - 按 `Ctrl+R` 或 F1 → "Developer: Reload Window"

3. **打开 q2 功能**
   - 按 `F2`

4. **查看开发者工具**
   - F1 → "Developer: Toggle Developer Tools"
   - 切换到 "Console" 标签

5. **观察日志输出**

应该看到类似这样的日志：

```
updateResourceExplorer: 开始更新，currentPath = C:\Users\...
directoryContents: {dirs: 5, files: 10}
updateResourceExplorer: 发送 update 消息，items = 16
fileListHtml 预览: <div class="file-item folder" ...
updateResourceExplorer: update 消息已发送
webview 收到消息: update
webview: 处理 update 消息
  currentPath: C:\Users\...
  fileListHtml 长度: 2345
  items 数量: 16
webview: fileList innerHTML 已设置
webview: update 处理完成
```

## 可能的问题

### 1. 如果看不到任何 Node.js 端的日志
- 说明 `updateResourceExplorer()` 根本没有被调用
- 检查 `setTimeout(() => updateResourceExplorer(), 100);` 是否执行

### 2. 如果看到 Node.js 日志但没有 webview 日志
- 说明消息没有到达 webview
- 可能是 webview 没有初始化完成
- 可能是 message 监听器没有注册

### 3. 如果看到 webview 日志但文件列表还是空
- 说明 `fileListHtml` 可能为空
- 检查 `directoryContents` 是否有内容
- 检查文件列表 HTML 生成逻辑

### 4. 如果 fileListHtml 长度为 0
- 检查当前路径是否存在
- 检查是否有权限读取目录
- 检查 `getDirectoryContents()` 函数是否正常工作

## 快速验证

打开开发者工具后，在 Console 输入：

```javascript
// 检查 fileList 元素是否存在
document.getElementById('fileList')

// 检查 fileList 的内容
document.getElementById('fileList').innerHTML

// 检查 addressInput 的值
document.getElementById('addressInput').value
```

---

**下一步**：根据日志输出来定位具体问题
